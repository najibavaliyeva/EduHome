@model List<SliderGetVM>


<div class="container">
<a class="btn btn-primary" asp-action="Create">Create</a>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Title</th>
                <th scope="col">Text</th>
                <th scope="col">Image</th>
                <th scope="col">Created at</th>
                <th scope="col">Updated at</th>
                <th scope="col">Buttons</th>


            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.Title</td>
                    <td>@item.Text</td>
                    <td><img style="width:100px; height:100px; object-fit:cover;"class="img-thumbnail" src="~/images/slider/@item.Image"/></td>
                    <td>@item.CreatedAt</td>
                    <td>@item.UpdatedAt</td>
                    <td class="align-middle text-center">
                        <div class="d-flex justify-content-center align-items-center gap-2">
                            <!-- Redaktə et (Düzəliş) -->
                            <a asp-action="update" asp-route-id="@item.Id" class="btn btn-sm btn-link p-0 text-warning" title="Redaktə et">
                                <i class="fa-solid fa-pen-to-square fa-lg"></i>
                            </a>

                            <!-- Sil (Form) -->
                            <form asp-action="remove" asp-route-id="@item.Id" class="m-0 d-inline" onsubmit="return confirm('Silmək istədiyinizdən əminsiniz?');">
                                <button type="submit" class="btn btn-sm btn-link p-0 text-danger" title="Sil">
                                    <i class="fa-solid fa-trash-can fa-lg"></i>
                                </button>
                            </form>
                        </div>
                    </td>


                </tr>
            }

        </tbody>
    </table>
    <a class="btn btn-default" asp asp-controller="dashboard" asp-action="index">Go Dashboard</a>
</div>